# Safe placeholders only. Do not commit real tokens or local secrets.

GITHUB_OWNER=TeamFloPay
GITHUB_PROJECT_NUMBER=1
GITHUB_PROJECT_TITLE=Campaign Map

# Local LLM launcher used by future War Room handoffs.
# Supported values for the MVP planning surface: codex, claude
LLM_ADAPTER=codex

# Optional command paths if they are not on PATH.
CODEX_COMMAND=codex
CLAUDE_COMMAND=claude

# Provider/API keys belong in .env.local, 1Password, Doppler, SOPS, or the
# relevant MCP/tool configuration. They must not be committed here.
