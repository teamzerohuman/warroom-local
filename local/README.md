# Local Orchestration

This directory is for local-only cross-repo helpers such as Caddy routes, Docker Compose wrappers, and future SDK-to-demo linking support.

Live/deployable infrastructure remains owned by the `TeamFloPay/infra` repository.
