# Maps

Maps describe child repositories and issue territory.

- `campaign-atlas.md` is the human-readable repo map and specialist roster.
- `issue-territory.md` summarizes current known work and dependencies.
- `repos/` is ignored and reserved for local child repository checkouts.

The machine-readable repo source of truth is `../repos.yaml`.
